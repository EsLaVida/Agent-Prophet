# 📈 Агент для прогнозирования временных рядов

Интеллектуальный агент на базе LangGraph и Prophet для прогнозирования временных рядов (продажи, поездки, цены) с использованием LLM для естественного общения с пользователем.

## 🚀 Возможности

- **Прогнозирование временных рядов** с использованием библиотеки Prophet (Facebook)
- **Естественный язык** - общение с агентом на русском языке
- **Поддержка нескольких рядов**: sales (продажи), trips (поездки), price (цена)
- **REST API** для интеграции с другими системами
- **Консольный режим** для интерактивного использования
- **Управление сессиями** с сохранением контекста диалога

## 🛠 Технологии

- **LangGraph** - для построения агентских графов
- **LangChain** - для работы с LLM
- **Prophet** - для прогнозирования временных рядов
- **FastAPI** - для REST API
- **OpenRouter** - для доступа к LLM моделям (MiMo-v2)

## 📋 Требования

- Python 3.11+
- API ключ OpenRouter (можно получить на [openrouter.ai](https://openrouter.ai))

## 🔧 Установка

1. Клонируйте репозиторий:
```bash
git clone <your-repo-url>
cd "Агент для прогнозирования по временным рядам"
```

2. Создайте виртуальное окружение:
```bash
python -m venv .venv
```

3. Активируйте виртуальное окружение:

**Windows:**
```bash
.venv\Scripts\activate
```

**Linux/Mac:**
```bash
source .venv/bin/activate
```

4. Установите зависимости:
```bash
pip install -r requirements.txt
```

**Примечание:** Установка Prophet на Windows может требовать C++ компилятор. Если возникают проблемы:
- Используйте conda: `conda install -c conda-forge prophet`
- Или установите [Microsoft C++ Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/)

5. Создайте файл `.env` в корне проекта:
```env
OPENAI_API_KEY=your_openrouter_api_key_here
```

6. Подготовьте CSV файлы с данными:
   - `sales.csv` - данные о продажах
   - `trips.csv` - данные о поездках
   - `price.csv` - данные о ценах

   Формат CSV файлов:
   ```csv
   ds,y
   2024-01-01,100
   2024-01-02,120
   ...
   ```
   Где `ds` - дата, `y` - значение временного ряда.

## 🎯 Использование

### Консольный режим

Запустите агента в интерактивном режиме:

```bash
python main.py
```

Примеры запросов:
- "Сколько будет продаж 15 февраля 2026?"
- "Прогноз поездок на послезавтра"
- "Какая будет цена на следующей неделе?"

### REST API

Запустите API сервер:

```bash
python run_api.py
```

Или с помощью uvicorn:

```bash
uvicorn run_api:app --reload
```

API будет доступен по адресу: `http://127.0.0.1:8000`

#### Пример запроса к API:

```bash
curl -X POST "http://127.0.0.1:8000/chat" \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Сколько будет продаж 15 февраля 2026?",
    "session_id": "optional-session-id"
  }'
```

#### Ответ API:

```json
{
  "reply": "Согласно моим расчетам, sales на дату 2026-02-15 составит примерно 150.",
  "session_id": "uuid-here",
  "payload": {
    "value": 150.5,
    "series": "sales",
    "target_date": "2026-02-15"
  }
}
```

## 📁 Структура проекта

```
.
├── config/              # Конфигурация
│   ├── prompts.py       # Системные промпты
│   └── settings.py      # Настройки (API ключи)
├── src/                 # Исходный код
│   ├── agent.py        # Граф агента (LangGraph)
│   ├── llm_client.py   # Клиент для работы с LLM
│   └── tools.py         # Инструменты агента
├── main.py             # Консольный интерфейс
├── run_api.py          # REST API сервер
├── requirements.txt    # Зависимости
├── .env                # Переменные окружения (не в git)
├── sales.csv           # Данные о продажах
├── trips.csv           # Данные о поездках
└── price.csv           # Данные о ценах
```

## 🔍 Как это работает

1. **Пользователь задает вопрос** на естественном языке
2. **LLM анализирует запрос** и определяет:
   - Тип временного ряда (sales/trips/price)
   - Целевую дату для прогноза
3. **Вызывается инструмент** `get_prediction` с параметрами
4. **Prophet загружает данные** из соответствующего CSV файла
5. **Модель обучается** на исторических данных
6. **Выполняется прогноз** на указанную дату
7. **Результат форматируется** и возвращается пользователю

## ⚙️ Настройка

### Изменение LLM модели

В файле `src/llm_client.py` можно изменить модель:

```python
llm = ChatOpenAI(
    model="xiaomi/mimo-v2-flash",  # Замените на другую модель
    base_url="https://openrouter.ai/api/v1",
    temperature=0.4,
    api_key=OPENAI_API_KEY
)
```

### Настройка Prophet

В файле `src/agent.py` можно настроить параметры Prophet:

```python
m = Prophet(
    yearly_seasonality=True,
    daily_seasonality=False,
    # Добавьте другие параметры
)
```

## 🐛 Решение проблем

### Ошибка установки Prophet на Windows

Если при установке возникает ошибка с длинными путями:
1. Включите поддержку длинных путей в Windows
2. Или используйте conda: `conda install -c conda-forge prophet`

### Ошибка "No such file or directory" для CSV

Убедитесь, что CSV файлы находятся в корне проекта и имеют правильный формат (столбцы `ds` и `y`).

### Ошибка API ключа

Проверьте, что файл `.env` создан и содержит правильный ключ:
```env
OPENAI_API_KEY=sk-or-v1-...
```

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Вклад

Приветствуются pull requests! Для крупных изменений сначала откройте issue для обсуждения.

## 📧 Контакты

Если у вас есть вопросы или предложения, создайте issue в репозитории.

